use winres::WindowsResource;

fn main() {
    if cfg!(target_os = "windows") {
        WindowsResource::new()
            .set_icon("./assets/prog_icon.ico")
            .compile()
            .unwrap();
    }
}
